spring.datasource.url=jdbc:mysql://192.168.40.129:3306/demo
spring.datasource.username=demo
spring.datasource.password=demopw

# 注意注意
#A、由于 mybatis.mapper-locations=classpath:com/nd/zsp/bootmybatisdemo/mapper/*.xml配置的在java package中，
#而Spring Boot默认只打入java package -> *.java，所以我们需要给pom.xml文件添加如下内容
#<build>
#<resources>
#<resource>
#<directory>src/main/resources</directory>
#</resource>
#<resource>
#<directory>src/main/java</directory>
#<includes>
#<include>**/*.xml</include>
#</includes>
#<filtering>true</filtering>
#</resource>
#</resources>
#<plugins>
#<plugin>
#<groupId>org.springframework.boot</groupId>
#<artifactId>spring-boot-maven-plugin</artifactId>
#</plugin>
#</plugins>
#</build>
mybatis.mapper-locations=classpath:com/nd/zsp/bootmybatisdemo/mapper/*.xml
#B、这种方式需要自己在resources目录下创建mapper目录然后存放xml
#mybatis.mapper-locations=classpath:mapper/*.xml
# 驼峰命名规范 如：数据库字段是  order_id 那么 实体字段就要写成 orderId
mybatis.configuration.map-underscore-to-camel-case=true

########## 通用Mapper ##########
# 主键自增回写方法,默认值MYSQL,详细说明请看文档
mapper.identity=MYSQL
mapper.mappers=tk.mybatis.mapper.common.BaseMapper
# 设置 insert 和 update 中，是否判断字符串类型!=''
mapper.not-empty=true
# 枚举按简单类型处理
mapper.enum-as-simple-type=true

########## 分页插件 ##########
pagehelper.helper-dialect=mysql
pagehelper.params=count=countSql
pagehelper.reasonable=false
pagehelper.support-methods-arguments=true

# 如果想看到mybatis日志需要做如下配置
logging.level.com.nd.zsp=DEBUG

# Redis
spring.redis.host=192.168.40.129
spring.redis.password=
# 连接超时时间（毫秒）
spring.redis.timeout=10000
# Redis默认情况下有16个分片，这里配置具体使用的分片，默认是0
spring.redis.database=0
# 连接池最大连接数（使用负值表示没有限制） 默认 8
spring.redis.lettuce.pool.max-active=8
# 连接池最大阻塞等待时间（使用负值表示没有限制） 默认 -1
spring.redis.lettuce.pool.max-wait=-1
# 连接池中的最大空闲连接 默认 8
spring.redis.lettuce.pool.max-idle=8
# 连接池中的最小空闲连接 默认 0
spring.redis.lettuce.pool.min-idle=0